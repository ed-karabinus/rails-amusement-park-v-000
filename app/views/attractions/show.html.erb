<p><%= @attraction.name %></p>

<% if @current_user.try(:admin) %>
  <%= link_to "Edit Attraction", edit_attraction_path(@attraction) %>
<% else %>
  <%= button_to "Go on this ride", url: take_ride_path(@attraction), method: :post %>
<% end %>